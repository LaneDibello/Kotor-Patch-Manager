# DLL-only patch.
# The runtime DLL detours OpenGL extension resolution, then detours the resolved
# glProgramStringARB and glBindProgramARB driver entry points so shader calls
# made through the game or other patch DLLs are tracked, dumped, and can load
# hash-named overrides from disk.
