Next:
- Test out the Update function to figure out the float cadence
- Ovewrite Draw for the panel to correctly render all the borders of the screen with the right fill image

- Capture the output of takeStep, and store a gameOver state for when the snake dies